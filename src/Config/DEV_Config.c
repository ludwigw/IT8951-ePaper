// This file is now a stub. See src/platform/DEV_Config_BCM.c, DEV_Config_LGPIO.c, or DEV_Config_GPIOD.c for platform-specific implementations.
// The build system should only compile the correct platform-specific file.

#error "Do not use DEV_Config.c directly. Use the appropriate platform-specific implementation."
